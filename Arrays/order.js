// store values in such a way that you can access the values in the order in which they were stored.

// create new array

let queue = new Array();

// push on three entries

queue.push('first');
queue.push('second');
queue.push('third');

console.log(queue.shift());
console.log(queue.shift());
console.log(queue);




