// Create a single string from an array.

let fruitArray = ['apple','peach','lemon','lime'];

// Use the array join method to join them into a single string

let resultString = fruitArray.join('-');
console.log(resultString);


let  numberArray = [1,2,3,4,5]; // array literal containing number elements
let resultNumString = numberArray.join('+');

console.log(resultNumString);

